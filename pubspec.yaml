name: dynamic_fonts
description: Lazy load fonts
version: 0.0.1
author: supernovel (supernovel@lucentblock.com)
homepage: https://lucentblock.com

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+2
  path_provider: '>=1.4.0 <3.0.0'

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
